

# Import the MyList class from the correct module
>>> my_list = __import__('1-my_list').MyList

# Create an instance of MyList
>>> my_list = MyList()

# Test print_sorted() on an empty list
# It should return an empty list as well
>>> my_list.print_sorted()
[]

# Append some integers to the list
>>> my_list.append(2)
>>> my_list.append(2)
>>> my_list.append(2)
>>> my_list.append(2)

# Test print_sorted() on a list with repeated integers
# It should return the integers in ascending order
>>> my_list.print_sorted()
[2, 2, 2, 2]

# Append more integers to the list
>>> my_list.append(1)

# Test print_sorted() on a list with mixed integers
# It should return the integers in ascending order
>>> my_list.print_sorted()
[1, 2, 2, 2, 2]

# Append more integers to the list
>>> my_list.append(10)
>>> my_list.append(0)
>>> my_list.append(-11)

# Test print_sorted() on a list with negative integers
# It should return the integers in ascending order
>>> my_list.print_sorted()
[-11, 0, 1, 2, 2, 2, 2, 10]

# Test printing the entire list using the print() function
# It should print the list as a regular list
>>> print(my_list)
[2, 2, 2, 2, 1, 10, 0, -11]

# Create another instance of MyList
>>> my_list2 = MyList()

# Append some negative integers to my_list2
>>> my_list2.append(-2)
>>> my_list2.append(-5)
>>> my_list2.append(-3)
>>> my_list2.append(-20)

# Test print_sorted() on my_list2
# It should return the negative integers in ascending order
>>> my_list2.print_sorted()
[-20, -5, -3, -2]

# Append more integers to my_list2
>>> my_list2.append(2)
>>> my_list2.append(-50)
>>> my_list2.append(0)

# Test print_sorted() on my_list2 with mixed integers
# It should return all integers in ascending order
>>> my_list2.print_sorted()
[-50, -20, -5, -3, -2, 0, 2]

# Test printing my_list2 using the print() function
# It should print my_list2 as a regular list
>>> print(my_list2)
[-2, -5, -3, -20, 2, -50, 0]

# Attempt to call print_sorted() with an extra argument
# It should raise a TypeError
>>> my_list2.print_sorted(my_list2)
Traceback (most recent call last):
TypeError: print_sorted() takes 1 positional argument but 2 were given

# Create another instance of MyList
>>> other = MyList()

# Append some integers to other
>>> other.append(1)
>>> other.append(2)
>>> other.append(3)
>>> other.append(4)
>>> other.append(5)
>>> other.append(6)

# Test print_sorted() on the "other" instance
# It should return all integers in ascending order
>>> other.print_sorted()
[1, 2, 3, 4, 5, 6]
